# Day 13: Buttons, Themes & Custom Styling

> **Aaj ka 2-hour plan:**
> - **Hour 1 (Concept):** ElevatedButton/TextButton/OutlinedButton/IconButton, ThemeData + ColorScheme, TextTheme, custom widgets
> - **Hour 2 (Practice):** App-wide theme, dark mode toggle, custom GradientButton, theme-driven buttons

**Phase:** Phase 2 — Mid

---

# 🌅 HOUR 1 — CONCEPT

## Button Types — 4 Main

### 1. ElevatedButton (Filled, Primary)

```dart
ElevatedButton(
  onPressed: () { print('clicked'); },
  child: const Text('Save'),
)

// Disabled
ElevatedButton(
  onPressed: null,        // null = disabled
  child: const Text('Save'),
)
```

### 2. TextButton (Plain Text)

```dart
TextButton(
  onPressed: () {},
  child: const Text('Cancel'),
)
```

### 3. OutlinedButton (Bordered)

```dart
OutlinedButton(
  onPressed: () {},
  child: const Text('Skip'),
)
```

### 4. IconButton (Icon Only)

```dart
IconButton(
  icon: const Icon(Icons.favorite),
  color: Colors.red,
  onPressed: () {},
)
```

> **Use Cases:**
> - **ElevatedButton**: Primary action (Save, Submit, Login)
> - **TextButton**: Secondary action (Cancel, Skip, Forgot Password)
> - **OutlinedButton**: Tertiary action (Sign in with Google, Less important)
> - **IconButton**: Toolbar icons, app bar actions

---

## Button Styling

### Style via Constructor

```dart
ElevatedButton(
  onPressed: () {},
  style: ElevatedButton.styleFrom(
    backgroundColor: Colors.indigo,
    foregroundColor: Colors.white,
    padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16),
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(12),
    ),
    elevation: 4,
    minimumSize: const Size(120, 48),
    textStyle: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
  ),
  child: const Text('Login'),
)
```

### Button.icon Variant

```dart
ElevatedButton.icon(
  onPressed: () {},
  icon: const Icon(Icons.send),
  label: const Text('Send'),
)

OutlinedButton.icon(
  onPressed: () {},
  icon: const Icon(Icons.share),
  label: const Text('Share'),
)
```

---

## ThemeData — App-wide Style

`MaterialApp` me theme set karke saare widgets ko consistent style milti hai.

```dart
MaterialApp(
  theme: ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo),
    useMaterial3: true,
  ),
  home: ...,
)
```

### ColorScheme.fromSeed (Material 3)

Ek **seed color** dene se Flutter automatically pura palette generate karta hai — primary, secondary, surface, error, etc.

```dart
ColorScheme.fromSeed(
  seedColor: Colors.indigo,
  brightness: Brightness.light,  // ya .dark
)
```

> **Tip:** **Material 3** (`useMaterial3: true`) modern look deta hai. Default Material 2 se shift karo new apps me.

### Manually ColorScheme

```dart
ColorScheme(
  brightness: Brightness.light,
  primary: Colors.indigo,
  onPrimary: Colors.white,
  secondary: Colors.amber,
  onSecondary: Colors.black,
  surface: Colors.white,
  onSurface: Colors.black87,
  error: Colors.red,
  onError: Colors.white,
)
```

---

## TextTheme — Typography

```dart
ThemeData(
  textTheme: const TextTheme(
    displayLarge: TextStyle(fontSize: 57, fontWeight: FontWeight.bold),
    displayMedium: TextStyle(fontSize: 45),
    displaySmall: TextStyle(fontSize: 36),
    headlineLarge: TextStyle(fontSize: 32, fontWeight: FontWeight.bold),
    headlineMedium: TextStyle(fontSize: 28),
    headlineSmall: TextStyle(fontSize: 24),
    titleLarge: TextStyle(fontSize: 22, fontWeight: FontWeight.w600),
    titleMedium: TextStyle(fontSize: 16, fontWeight: FontWeight.w500),
    bodyLarge: TextStyle(fontSize: 16),
    bodyMedium: TextStyle(fontSize: 14),
    labelLarge: TextStyle(fontSize: 14, fontWeight: FontWeight.w500),
  ),
)

// Use
Text(
  'Heading',
  style: Theme.of(context).textTheme.headlineLarge,
)
```

---

## Component Themes

App-wide button/input/card style:

```dart
ThemeData(
  // ElevatedButton ka default style
  elevatedButtonTheme: ElevatedButtonThemeData(
    style: ElevatedButton.styleFrom(
      padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
      textStyle: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
    ),
  ),

  // Input field ka default style
  inputDecorationTheme: InputDecorationTheme(
    filled: true,
    fillColor: Colors.grey[100],
    border: OutlineInputBorder(
      borderRadius: BorderRadius.circular(12),
      borderSide: BorderSide.none,
    ),
  ),

  // Card ka default style
  cardTheme: const CardTheme(
    elevation: 2,
    margin: EdgeInsets.all(8),
  ),

  // AppBar
  appBarTheme: const AppBarTheme(
    backgroundColor: Colors.indigo,
    foregroundColor: Colors.white,
    elevation: 0,
    centerTitle: true,
  ),
)
```

---

## Light + Dark Theme

```dart
MaterialApp(
  theme: ThemeData.light(useMaterial3: true).copyWith(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo),
  ),
  darkTheme: ThemeData.dark(useMaterial3: true).copyWith(
    colorScheme: ColorScheme.fromSeed(
      seedColor: Colors.indigo,
      brightness: Brightness.dark,
    ),
  ),
  themeMode: ThemeMode.system,  // ya .light, .dark
)
```

---

## Custom Widget — GradientButton

```dart
class GradientButton extends StatelessWidget {
  final String text;
  final VoidCallback onPressed;
  final List<Color> colors;

  const GradientButton({
    super.key,
    required this.text,
    required this.onPressed,
    this.colors = const [Colors.indigo, Colors.purple],
  });

  @override
  Widget build(BuildContext context) {
    return InkWell(
      onTap: onPressed,
      borderRadius: BorderRadius.circular(12),
      child: Container(
        padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16),
        decoration: BoxDecoration(
          gradient: LinearGradient(colors: colors),
          borderRadius: BorderRadius.circular(12),
          boxShadow: [
            BoxShadow(
              color: colors.first.withOpacity(0.3),
              blurRadius: 8,
              offset: const Offset(0, 4),
            ),
          ],
        ),
        child: Text(
          text,
          style: const TextStyle(
            color: Colors.white,
            fontSize: 16,
            fontWeight: FontWeight.bold,
          ),
        ),
      ),
    );
  }
}
```

---

# 🌆 HOUR 2 — PRACTICE

## Task 1: App-Wide Theme Setup (15 min)

```dart
import 'package:flutter/material.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Theme Demo',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: Colors.indigo,
          brightness: Brightness.light,
        ),
        useMaterial3: true,

        elevatedButtonTheme: ElevatedButtonThemeData(
          style: ElevatedButton.styleFrom(
            padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
            shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
            textStyle: const TextStyle(fontSize: 16, fontWeight: FontWeight.bold),
          ),
        ),

        inputDecorationTheme: InputDecorationTheme(
          filled: true,
          fillColor: Colors.grey[100],
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(12),
            borderSide: BorderSide.none,
          ),
        ),

        appBarTheme: const AppBarTheme(centerTitle: true, elevation: 0),
      ),
      home: const ThemeShowcase(),
    );
  }
}

class ThemeShowcase extends StatelessWidget {
  const ThemeShowcase({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Theme Showcase')),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            // Buttons section
            Text('Buttons', style: Theme.of(context).textTheme.headlineSmall),
            const SizedBox(height: 12),
            ElevatedButton(onPressed: () {}, child: const Text('Elevated')),
            const SizedBox(height: 8),
            OutlinedButton(onPressed: () {}, child: const Text('Outlined')),
            const SizedBox(height: 8),
            TextButton(onPressed: () {}, child: const Text('Text Button')),
            const SizedBox(height: 8),
            ElevatedButton.icon(
              onPressed: () {},
              icon: const Icon(Icons.send),
              label: const Text('Send Message'),
            ),

            const Divider(height: 32),

            // Inputs
            Text('Inputs', style: Theme.of(context).textTheme.headlineSmall),
            const SizedBox(height: 12),
            const TextField(decoration: InputDecoration(hintText: 'Email')),
            const SizedBox(height: 12),
            const TextField(decoration: InputDecoration(hintText: 'Password'), obscureText: true),
          ],
        ),
      ),
    );
  }
}
```

---

## Task 2: Dark Mode Toggle (20 min)

```dart
class ThemeApp extends StatefulWidget {
  const ThemeApp({super.key});

  @override
  State<ThemeApp> createState() => _ThemeAppState();
}

class _ThemeAppState extends State<ThemeApp> {
  bool _isDark = false;

  ThemeData _buildTheme(Brightness b) => ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo, brightness: b),
    useMaterial3: true,
    elevatedButtonTheme: ElevatedButtonThemeData(
      style: ElevatedButton.styleFrom(
        padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 14),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
      ),
    ),
  );

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      theme: _buildTheme(Brightness.light),
      darkTheme: _buildTheme(Brightness.dark),
      themeMode: _isDark ? ThemeMode.dark : ThemeMode.light,
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Theme Toggle'),
          actions: [
            IconButton(
              icon: Icon(_isDark ? Icons.light_mode : Icons.dark_mode),
              onPressed: () => setState(() => _isDark = !_isDark),
            ),
          ],
        ),
        body: Center(
          child: Padding(
            padding: const EdgeInsets.all(20),
            child: Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Icon(
                  _isDark ? Icons.nightlight_round : Icons.wb_sunny,
                  size: 100,
                  color: Theme.of(context).colorScheme.primary,
                ),
                const SizedBox(height: 24),
                Text(
                  _isDark ? 'Dark Mode' : 'Light Mode',
                  style: Theme.of(context).textTheme.headlineMedium,
                ),
                const SizedBox(height: 16),
                Text(
                  'Top right toggle pe click karo',
                  style: TextStyle(color: Theme.of(context).colorScheme.onSurfaceVariant),
                ),
                const SizedBox(height: 32),
                ElevatedButton(onPressed: () {}, child: const Text('Sample Button')),
              ],
            ),
          ),
        ),
      ),
    );
  }
}
```

> **Note:** Day 18 me SharedPreferences se theme persist karna sikhenge. Abhi sirf in-memory.

---

## Task 3: Custom GradientButton Widget (15 min)

```dart
class GradientButton extends StatelessWidget {
  final String text;
  final IconData? icon;
  final VoidCallback onPressed;
  final List<Color> colors;
  final double height;

  const GradientButton({
    super.key,
    required this.text,
    required this.onPressed,
    this.icon,
    this.colors = const [Color(0xFF6366F1), Color(0xFF8B5CF6)],
    this.height = 56,
  });

  @override
  Widget build(BuildContext context) {
    return InkWell(
      onTap: onPressed,
      borderRadius: BorderRadius.circular(12),
      child: Container(
        height: height,
        padding: const EdgeInsets.symmetric(horizontal: 24),
        decoration: BoxDecoration(
          gradient: LinearGradient(
            colors: colors,
            begin: Alignment.topLeft,
            end: Alignment.bottomRight,
          ),
          borderRadius: BorderRadius.circular(12),
          boxShadow: [
            BoxShadow(
              color: colors.first.withOpacity(0.4),
              blurRadius: 12,
              offset: const Offset(0, 4),
            ),
          ],
        ),
        child: Row(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            if (icon != null) ...[
              Icon(icon, color: Colors.white, size: 20),
              const SizedBox(width: 8),
            ],
            Text(
              text,
              style: const TextStyle(
                color: Colors.white,
                fontSize: 16,
                fontWeight: FontWeight.bold,
                letterSpacing: 0.5,
              ),
            ),
          ],
        ),
      ),
    );
  }
}

// Use
Column(
  children: [
    GradientButton(text: 'Login', onPressed: () {}),
    SizedBox(height: 12),
    GradientButton(
      text: 'Sign Up',
      icon: Icons.person_add,
      colors: [Colors.green, Colors.teal],
      onPressed: () {},
    ),
    SizedBox(height: 12),
    GradientButton(
      text: 'Continue with Google',
      icon: Icons.g_mobiledata,
      colors: [Colors.red, Colors.orange],
      onPressed: () {},
    ),
  ],
)
```

---

## Task 4: Theme.of(context) Everywhere (10 min)

```dart
class ThemedCard extends StatelessWidget {
  final String title;
  final String subtitle;

  const ThemedCard({super.key, required this.title, required this.subtitle});

  @override
  Widget build(BuildContext context) {
    final colors = Theme.of(context).colorScheme;
    final text = Theme.of(context).textTheme;

    return Container(
      padding: const EdgeInsets.all(20),
      decoration: BoxDecoration(
        color: colors.surfaceVariant,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(title, style: text.titleLarge?.copyWith(color: colors.primary)),
          const SizedBox(height: 4),
          Text(subtitle, style: text.bodyMedium?.copyWith(color: colors.onSurfaceVariant)),
        ],
      ),
    );
  }
}
```

> **Why?** Hardcoded colors `Colors.indigo` use karoge to dark mode me bekaar dikhega. `Theme.of(context).colorScheme.primary` use karo — automatically light/dark me adjust.

---

## Task 5: Git Commit (5 min)

```bash
git add .
git commit -m "Day 13: Buttons, themes, dark mode, gradient button"
git push
```

---

## 📋 Quick Revision

| Concept | Code |
|---------|------|
| Filled button | `ElevatedButton(onPressed: ..., child: ...)` |
| Plain | `TextButton(...)` |
| Bordered | `OutlinedButton(...)` |
| Icon only | `IconButton(icon: ..., onPressed: ...)` |
| With icon | `ElevatedButton.icon(icon: ..., label: ...)` |
| Custom style | `style: ElevatedButton.styleFrom(...)` |
| App theme | `theme: ThemeData(colorScheme: ..., useMaterial3: true)` |
| M3 colors | `ColorScheme.fromSeed(seedColor: ...)` |
| Light + Dark | `theme: ..., darkTheme: ..., themeMode: ...` |
| Read theme | `Theme.of(context).colorScheme.primary` |
| Read text style | `Theme.of(context).textTheme.headlineLarge` |

---

## ❌ Common Mistakes

```dart
// MISTAKE 1: Hardcoded colors (no dark mode support)
Container(color: Colors.white)  // dark mode me white background = bad
// FIX: Theme.of(context).colorScheme.surface

// MISTAKE 2: onPressed: () {} (empty)
ElevatedButton(onPressed: () {}, child: ...)  // shows enabled but does nothing
// Better: actually disable with null OR add real action

// MISTAKE 3: Theme.of(context) inside MaterialApp's home builder
home: Theme.of(context)...  // ❌ context me theme nahi hai abhi

// MISTAKE 4: Different style har button me copy
ElevatedButton(style: ..., ...)
ElevatedButton(style: ..., ...)  // repeat
// FIX: ElevatedButtonThemeData app-wide
```

---

## 🎯 Interview Tips

1. **Q: Material 2 vs Material 3?**
   *A: Material 3 (M3) Google ka latest design system — dynamic colors, expressive UI. `useMaterial3: true` enable karta hai.*

2. **Q: ColorScheme.fromSeed kya karta hai?**
   *A: Ek seed color se pura palette generate karta hai (primary, secondary, surface, error, etc.) automatically.*

3. **Q: Theme.of(context) kab use karna chahiye?**
   *A: Hardcoded colors/sizes ki jagah — taaki dark mode + theme switching automatic ho.*

4. **Q: ElevatedButton aur FilledButton me kya farq?**
   *A: Material 3 me FilledButton bhi available hai — flat colored button without elevation. ElevatedButton has shadow.*

---

## ✅ Aaj Kya Sikha

✅ ElevatedButton, TextButton, OutlinedButton, IconButton
✅ Button.icon variants
✅ ElevatedButton.styleFrom() customization
✅ ThemeData + ColorScheme.fromSeed (Material 3)
✅ TextTheme typography scale
✅ Component themes (button/input/card)
✅ Light + Dark theme switching
✅ Theme.of(context) for theme-aware widgets
✅ Custom GradientButton

**Kal (Day 14):** Navigation — multi-screen apps. Push/Pop, named routes, pass data.
