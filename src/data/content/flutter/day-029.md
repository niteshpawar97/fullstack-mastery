# Day 29: Capstone Day 4 — Animations, Testing & Polish

> **Aaj ka 2-hour plan:**
> - **Hour 1:** Hero animations, loading/empty/error states polish, splash screen
> - **Hour 2:** Widget tests (3-4), real device test, accessibility checks

**Phase:** Phase 3 — Advanced (Capstone Day 4 of 5)

---

# 🌅 HOUR 1 — POLISH

## Step 1: Splash Screen

```dart
// screens/splash_screen.dart
import 'package:flutter/material.dart';
import 'package:firebase_auth/firebase_auth.dart';
import 'auth/login_screen.dart';
import 'home_screen.dart';

class SplashScreen extends StatefulWidget {
  const SplashScreen({super.key});

  @override
  State<SplashScreen> createState() => _SplashScreenState();
}

class _SplashScreenState extends State<SplashScreen> {
  double _scale = 0.5;
  double _opacity = 0;

  @override
  void initState() {
    super.initState();
    Future.delayed(const Duration(milliseconds: 100), () {
      setState(() {
        _scale = 1;
        _opacity = 1;
      });
    });
    Future.delayed(const Duration(seconds: 2), _navigate);
  }

  void _navigate() {
    if (!mounted) return;
    final user = FirebaseAuth.instance.currentUser;
    Navigator.pushReplacement(
      context,
      MaterialPageRoute(builder: (_) => user != null ? const HomeScreen() : const LoginScreen()),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFFFF6B35),
      body: Center(
        child: AnimatedScale(
          scale: _scale,
          duration: const Duration(milliseconds: 800),
          curve: Curves.easeOutBack,
          child: AnimatedOpacity(
            opacity: _opacity,
            duration: const Duration(milliseconds: 800),
            child: const Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                Text('🍔', style: TextStyle(fontSize: 100)),
                SizedBox(height: 16),
                Text('Food Ordering', style: TextStyle(color: Colors.white, fontSize: 32, fontWeight: FontWeight.bold)),
                SizedBox(height: 8),
                Text('Tasty food at your doorstep', style: TextStyle(color: Colors.white70)),
              ],
            ),
          ),
        ),
      ),
    );
  }
}
```

Update `main.dart` home → `SplashScreen()`.

---

## Step 2: Polished Empty States

```dart
// widgets/empty_state.dart
class EmptyState extends StatelessWidget {
  final IconData icon;
  final String title;
  final String message;
  final String? actionLabel;
  final VoidCallback? onAction;

  const EmptyState({
    super.key,
    required this.icon,
    required this.title,
    required this.message,
    this.actionLabel,
    this.onAction,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Padding(
        padding: const EdgeInsets.all(40),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Icon(icon, size: 100, color: Colors.grey[300]),
            const SizedBox(height: 16),
            Text(title, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),
            const SizedBox(height: 8),
            Text(message, textAlign: TextAlign.center, style: TextStyle(color: Colors.grey[600])),
            if (actionLabel != null) ...[
              const SizedBox(height: 24),
              ElevatedButton(onPressed: onAction, child: Text(actionLabel!)),
            ],
          ],
        ),
      ),
    );
  }
}
```

Use:

```dart
// orders_screen.dart
if (orders.isEmpty) {
  return EmptyState(
    icon: Icons.receipt_long_outlined,
    title: 'No orders yet',
    message: 'Pehla order place karo aur yahan dikhega',
    actionLabel: 'Browse Restaurants',
    onAction: () => Navigator.pop(context),
  );
}
```

---

## Step 3: Shimmer Loading (Skeleton)

```bash
flutter pub add shimmer
```

```dart
// widgets/restaurant_card_shimmer.dart
import 'package:shimmer/shimmer.dart';

class RestaurantCardShimmer extends StatelessWidget {
  const RestaurantCardShimmer({super.key});

  @override
  Widget build(BuildContext context) {
    return Shimmer.fromColors(
      baseColor: Colors.grey[300]!,
      highlightColor: Colors.grey[100]!,
      child: Card(
        margin: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(width: double.infinity, height: 180, color: Colors.white),
            Padding(
              padding: const EdgeInsets.all(12),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Container(width: 200, height: 18, color: Colors.white),
                  const SizedBox(height: 8),
                  Container(width: 100, height: 14, color: Colors.white),
                  const SizedBox(height: 8),
                  Container(width: 150, height: 14, color: Colors.white),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

Use in HomeScreen loading:

```dart
loading: () => ListView.builder(
  itemCount: 5,
  itemBuilder: (_, __) => const RestaurantCardShimmer(),
),
```

---

## Step 4: Hero Animations Polish

Already added in `RestaurantCard` (Day 27). Confirm tag uniqueness:
```dart
Hero(tag: 'restaurant-${restaurant.id}', child: ...)
```

In `RestaurantDetailScreen`:
```dart
SliverAppBar(
  flexibleSpace: FlexibleSpaceBar(
    background: Hero(
      tag: 'restaurant-${restaurant.id}',
      child: CachedNetworkImage(imageUrl: restaurant.image, fit: BoxFit.cover),
    ),
  ),
)
```

---

## Step 5: Error Boundary

Wrap StreamProvider errors gracefully:

```dart
// widgets/error_view.dart
class ErrorView extends StatelessWidget {
  final String message;
  final VoidCallback? onRetry;

  const ErrorView({super.key, required this.message, this.onRetry});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Padding(
        padding: const EdgeInsets.all(40),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.error_outline, size: 100, color: Colors.red),
            const SizedBox(height: 16),
            const Text('Kuch galat ho gaya', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
            const SizedBox(height: 8),
            Text(message, textAlign: TextAlign.center, style: TextStyle(color: Colors.grey[600])),
            if (onRetry != null) ...[
              const SizedBox(height: 24),
              ElevatedButton.icon(
                onPressed: onRetry,
                icon: const Icon(Icons.refresh),
                label: const Text('Retry'),
              ),
            ],
          ],
        ),
      ),
    );
  }
}
```

---

# 🌆 HOUR 2 — TESTING

## Step 6: Widget Tests

```dart
// test/widget_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:food_ordering/widgets/empty_state.dart';

void main() {
  group('EmptyState Widget', () {
    testWidgets('shows title and message', (tester) async {
      await tester.pumpWidget(
        const MaterialApp(
          home: Scaffold(
            body: EmptyState(
              icon: Icons.shopping_cart,
              title: 'Cart Empty',
              message: 'Add items to get started',
            ),
          ),
        ),
      );

      expect(find.text('Cart Empty'), findsOneWidget);
      expect(find.text('Add items to get started'), findsOneWidget);
      expect(find.byIcon(Icons.shopping_cart), findsOneWidget);
    });

    testWidgets('shows action button when provided', (tester) async {
      var called = false;

      await tester.pumpWidget(
        MaterialApp(
          home: Scaffold(
            body: EmptyState(
              icon: Icons.search,
              title: 'No results',
              message: 'Try different search',
              actionLabel: 'Clear',
              onAction: () => called = true,
            ),
          ),
        ),
      );

      expect(find.text('Clear'), findsOneWidget);
      await tester.tap(find.text('Clear'));
      expect(called, isTrue);
    });
  });
}
```

---

## Step 7: Cart Logic Test

```dart
// test/cart_provider_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:food_ordering/providers/cart_provider.dart';
import 'package:food_ordering/models/menu_item.dart';

void main() {
  group('CartNotifier', () {
    late ProviderContainer container;

    setUp(() {
      container = ProviderContainer();
    });

    tearDown(() {
      container.dispose();
    });

    final pizza = MenuItem(id: '1', name: 'Pizza', description: '', price: 250, image: '', category: 'main', isVeg: true);
    final burger = MenuItem(id: '2', name: 'Burger', description: '', price: 150, image: '', category: 'main', isVeg: false);

    test('starts empty', () {
      final cart = container.read(cartProvider);
      expect(cart, isEmpty);
    });

    test('adds new item', () {
      container.read(cartProvider.notifier).add(pizza);
      final cart = container.read(cartProvider);
      expect(cart.length, 1);
      expect(cart.first.item.id, '1');
      expect(cart.first.quantity, 1);
    });

    test('increments quantity for existing item', () {
      final notifier = container.read(cartProvider.notifier);
      notifier.add(pizza);
      notifier.add(pizza);
      final cart = container.read(cartProvider);
      expect(cart.length, 1);
      expect(cart.first.quantity, 2);
    });

    test('calculates total correctly', () {
      final notifier = container.read(cartProvider.notifier);
      notifier.add(pizza);  // 250
      notifier.add(pizza);  // +250 = 500
      notifier.add(burger); // +150 = 650
      expect(container.read(cartTotalProvider), 650);
    });

    test('decrement removes item if quantity reaches 0', () {
      final notifier = container.read(cartProvider.notifier);
      notifier.add(pizza);
      notifier.decrementQty('1');
      final cart = container.read(cartProvider);
      expect(cart, isEmpty);
    });

    test('clear removes all', () {
      final notifier = container.read(cartProvider.notifier);
      notifier.add(pizza);
      notifier.add(burger);
      notifier.clear();
      expect(container.read(cartProvider), isEmpty);
    });
  });
}
```

Run:

```bash
flutter test
```

---

## Step 8: Accessibility Check

Add semantic labels:

```dart
IconButton(
  icon: const Icon(Icons.shopping_cart),
  onPressed: ...,
  tooltip: 'View cart',     // accessibility hint
)

Image.network(
  url,
  semanticLabel: 'Restaurant ${restaurant.name} cover photo',
)

Container(
  // ...
  child: Semantics(
    label: 'Cart total ${total} rupees',
    child: Text('₹${total}'),
  ),
)
```

> **Test:** Android me TalkBack on, iOS me VoiceOver on — har screen pe navigate karo.

---

## Step 9: Real Device Testing

```bash
flutter run --release
```

**Checklist:**
- ✅ Splash 2 sec dikha then auto-route
- ✅ Login → home animation smooth
- ✅ Restaurant → menu Hero animation
- ✅ Cart → order place → tracking
- ✅ Real-time status update from Firestore Console
- ✅ Logout works
- ✅ Phone back button navigation correct
- ✅ Slow network — loading states dikhe
- ✅ Internet off — error message + retry
- ✅ Permissions (camera/location if used) handle gracefully
- ✅ Different screen sizes (phone, tablet, foldable)

---

## Step 10: Performance Check

```bash
flutter run --profile
```

DevTools open → check:
- **Frames** — 60fps maintain ho rahi?
- **Memory** — leaks (continuously rising)?
- **Network** — slow image loads?

---

## Step 11: Bug Fixing Common Issues

```dart
// Issue 1: setState after dispose
if (mounted) setState(...);

// Issue 2: Image error handling
CachedNetworkImage(
  errorWidget: (_, __, ___) => const Icon(Icons.broken_image),
)

// Issue 3: Long restaurant names overflow
Text(name, maxLines: 1, overflow: TextOverflow.ellipsis)

// Issue 4: Cart empty after order — verify navigator pushReplacement
Navigator.pushReplacement(...);  // not push (cart wapas mat aaye)

// Issue 5: Status timeline not animating
// AnimatedContainer used? duration set?
```

---

## Step 12: Git Commit

```bash
git add .
git commit -m "Capstone Day 4: Splash, polish, animations, widget + unit tests"
git push
```

---

## ✅ Aaj Kya Built

✅ Splash screen with scale + fade animation
✅ EmptyState reusable widget
✅ Shimmer loading skeleton
✅ ErrorView with retry button
✅ Hero animations polished
✅ Widget tests for EmptyState
✅ Unit tests for CartNotifier (add, increment, total, clear)
✅ Accessibility — tooltips, semantic labels
✅ Real device testing checklist
✅ Performance profiling

**Kal (Day 30):** **FINAL DAY** — Sign APK + Play Store internal testing deploy. App publish hoga!
