# Day 42: Flutter Web — Responsive + Vercel Deploy

> **Aaj ka 2-hour plan:**
> - **Hour 1:** flutter build web, responsive layouts (LayoutBuilder, MediaQuery), web concerns (SEO, routing), CanvasKit vs HTML
> - **Hour 2:** Convert app to web, responsive breakpoints, deploy to Vercel

**Phase:** Phase 4 — BONUS

---

# 🌅 HOUR 1 — CONCEPT

## Flutter Web

Same Dart code se browser me bhi app chal sakta hai — same widgets, no rewrite.

> **Production users:** Google Pay, Rive, Wix Studio.

---

## Enable + Run

```bash
flutter config --enable-web
flutter create . --platforms=web
flutter run -d chrome
flutter build web --release
```

Output: `build/web/` — static HTML/JS/CSS.

---

## Renderers

| | CanvasKit | HTML |
|---|---|---|
| Visual | Same as native | Some differences |
| Bundle size | +2MB | Smaller |
| Performance | Better | Worse for animations |
| Default | Mobile + Desktop | Old default |

```bash
flutter build web --web-renderer canvaskit       # default
flutter build web --web-renderer html
flutter build web --web-renderer auto            # mobile=html, desktop=canvaskit
```

---

## Responsive — LayoutBuilder

```dart
LayoutBuilder(
  builder: (context, constraints) {
    if (constraints.maxWidth >= 1200) return DesktopLayout();
    if (constraints.maxWidth >= 600) return TabletLayout();
    return MobileLayout();
  },
)
```

### MediaQuery

```dart
final width = MediaQuery.of(context).size.width;
final isDesktop = width >= 1200;
```

### Breakpoint Helper

```dart
class Breakpoints {
  static const double mobile = 600;
  static const double tablet = 1200;

  static bool isMobile(BuildContext c) => MediaQuery.of(c).size.width < mobile;
  static bool isDesktop(BuildContext c) => MediaQuery.of(c).size.width >= tablet;
}
```

---

## Adaptive Widget

```dart
class AdaptiveLayout extends StatelessWidget {
  final Widget mobile;
  final Widget? tablet;
  final Widget desktop;

  const AdaptiveLayout({super.key, required this.mobile, this.tablet, required this.desktop});

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(builder: (context, constraints) {
      if (constraints.maxWidth >= 1200) return desktop;
      if (constraints.maxWidth >= 600) return tablet ?? mobile;
      return mobile;
    });
  }
}
```

---

## Web Concerns

### URL Routing — go_router

```bash
flutter pub add go_router
```

```dart
final router = GoRouter(routes: [
  GoRoute(path: '/', builder: (_, __) => HomeScreen()),
  GoRoute(path: '/about', builder: (_, __) => AboutScreen()),
  GoRoute(path: '/profile/:id', builder: (_, state) => ProfileScreen(id: state.pathParameters['id']!)),
]);

MaterialApp.router(routerConfig: router)
```

### CORS for Images

`Image.network` web me CORS issue. **`cached_network_image`** handles it.

### Browser Storage

`shared_preferences` web me localStorage uses karta — 5MB limit, user can clear.

### SEO

Flutter web SEO weak (canvas rendering). Marketing pages traditional HTML rakho, app `/app` route pe.

---

# 🌆 HOUR 2 — PRACTICE

## Task 1: Responsive Adaptive App (40 min)

```dart
import 'package:flutter/material.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      theme: ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo)),
      home: const ResponsiveScreen(),
    );
  }
}

class ResponsiveScreen extends StatelessWidget {
  const ResponsiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: LayoutBuilder(builder: (_, constraints) {
        if (constraints.maxWidth >= 1200) return const _DesktopLayout();
        if (constraints.maxWidth >= 600) return const _TabletLayout();
        return const _MobileLayout();
      }),
    );
  }
}

class _MobileLayout extends StatefulWidget {
  const _MobileLayout();
  @override
  State<_MobileLayout> createState() => _MobileLayoutState();
}

class _MobileLayoutState extends State<_MobileLayout> {
  int _index = 0;
  final pages = const [
    _PageContent(title: 'Home', icon: Icons.home),
    _PageContent(title: 'Search', icon: Icons.search),
    _PageContent(title: 'Profile', icon: Icons.person),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: pages[_index],
      bottomNavigationBar: BottomNavigationBar(
        currentIndex: _index,
        onTap: (i) => setState(() => _index = i),
        items: const [
          BottomNavigationBarItem(icon: Icon(Icons.home), label: 'Home'),
          BottomNavigationBarItem(icon: Icon(Icons.search), label: 'Search'),
          BottomNavigationBarItem(icon: Icon(Icons.person), label: 'Profile'),
        ],
      ),
    );
  }
}

class _TabletLayout extends StatefulWidget {
  const _TabletLayout();
  @override
  State<_TabletLayout> createState() => _TabletLayoutState();
}

class _TabletLayoutState extends State<_TabletLayout> {
  int _index = 0;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [
          NavigationRail(
            selectedIndex: _index,
            onDestinationSelected: (i) => setState(() => _index = i),
            labelType: NavigationRailLabelType.all,
            destinations: const [
              NavigationRailDestination(icon: Icon(Icons.home), label: Text('Home')),
              NavigationRailDestination(icon: Icon(Icons.search), label: Text('Search')),
              NavigationRailDestination(icon: Icon(Icons.person), label: Text('Profile')),
            ],
          ),
          const VerticalDivider(width: 1),
          Expanded(
            child: const [
              _PageContent(title: 'Home', icon: Icons.home),
              _PageContent(title: 'Search', icon: Icons.search),
              _PageContent(title: 'Profile', icon: Icons.person),
            ][_index],
          ),
        ],
      ),
    );
  }
}

class _DesktopLayout extends StatefulWidget {
  const _DesktopLayout();
  @override
  State<_DesktopLayout> createState() => _DesktopLayoutState();
}

class _DesktopLayoutState extends State<_DesktopLayout> {
  int _index = 0;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [
          Container(
            width: 280,
            color: Colors.indigo[50],
            child: ListView(
              children: [
                Container(
                  padding: const EdgeInsets.all(20),
                  child: const Text('My App', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
                ),
                ListTile(leading: const Icon(Icons.home), title: const Text('Home'), selected: _index == 0, onTap: () => setState(() => _index = 0)),
                ListTile(leading: const Icon(Icons.search), title: const Text('Search'), selected: _index == 1, onTap: () => setState(() => _index = 1)),
                ListTile(leading: const Icon(Icons.person), title: const Text('Profile'), selected: _index == 2, onTap: () => setState(() => _index = 2)),
              ],
            ),
          ),
          const VerticalDivider(width: 1),
          Expanded(
            child: const [
              _PageContent(title: 'Home', icon: Icons.home),
              _PageContent(title: 'Search', icon: Icons.search),
              _PageContent(title: 'Profile', icon: Icons.person),
            ][_index],
          ),
        ],
      ),
    );
  }
}

class _PageContent extends StatelessWidget {
  final String title;
  final IconData icon;
  const _PageContent({required this.title, required this.icon});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(24),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Icon(icon, size: 100, color: Colors.indigo),
          const SizedBox(height: 16),
          Text(title, style: const TextStyle(fontSize: 32, fontWeight: FontWeight.bold)),
          const SizedBox(height: 8),
          Text('Width: ${MediaQuery.of(context).size.width.toStringAsFixed(0)}px'),
        ],
      ),
    );
  }
}
```

> **Test:** Browser me khol ke window resize karo — mobile/tablet/desktop layouts switch.

---

## Task 2: Build + Deploy to Vercel (15 min)

```bash
flutter build web --release
```

### Via Vercel CLI

```bash
npm install -g vercel
cd build/web
vercel login
vercel --prod
```

### Via GitHub Integration

1. Push to GitHub
2. [vercel.com/new](https://vercel.com/new) → Import repo
3. Build settings:
   - **Framework:** Other
   - **Build Command:** `flutter build web --release`
   - **Output Directory:** `build/web`
4. Auto-deploy on push.

### vercel.json (SPA Routing)

`build/web/vercel.json`:

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

> **Why?** SPA. All routes → `index.html`. Without — direct visit `/about` me 404.

---

## Task 3: Git Commit (5 min)

```bash
git add .
git commit -m "Day 42: Flutter Web responsive + Vercel deploy"
git push
```

---

## 📋 Quick Revision

| Action | Code |
|--------|------|
| Run web | `flutter run -d chrome` |
| Build | `flutter build web --release` |
| Renderer | `--web-renderer canvaskit/html/auto` |
| Responsive | `LayoutBuilder` |
| Routing | `go_router` |
| Deploy | Vercel/Netlify |
| SPA fix | `vercel.json` rewrites |

---

## 🎯 Interview Tips

1. **Q: Flutter web production-ready?**
   *A: Internal tools + dashboards yes. Public sites SEO weak.*

2. **Q: Responsive layout strategy?**
   *A: LayoutBuilder + breakpoints (600/1200) + AdaptiveLayout pattern.*

3. **Q: NetworkImage CORS issue?**
   *A: cached_network_image use karo, ya server CORS headers configure.*

---

## ✅ Aaj Kya Sikha

✅ Flutter Web setup + build
✅ Renderers (CanvasKit vs HTML)
✅ LayoutBuilder + MediaQuery
✅ Mobile/Tablet/Desktop adaptive
✅ go_router for proper URLs
✅ Vercel deployment
✅ SPA rewrites

**Kal (Day 43):** Flutter Desktop (Windows/macOS/Linux).
