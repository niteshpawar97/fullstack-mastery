# Day 44: Internationalization (i18n) + Accessibility (a11y)

> **Aaj ka 2-hour plan:**
> - **Hour 1:** flutter_localizations, ARB files, locale switching, semantic labels, contrast, touch targets
> - **Hour 2:** Hindi/English support, locale toggle in settings, semantic widgets, TalkBack/VoiceOver test

**Phase:** Phase 4 — BONUS

---

# 🌅 HOUR 1 — CONCEPT

## Internationalization (i18n)

App ko **multiple languages** me support karna. Indian apps me Hindi + English minimum.

---

## Setup

```yaml
# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

flutter:
  generate: true   # auto-generate from ARB files
```

`l10n.yaml` (project root):

```yaml
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
```

---

## ARB Files (Translation Strings)

`lib/l10n/app_en.arb`:

```json
{
  "@@locale": "en",
  "appTitle": "Flutter App",
  "@appTitle": {
    "description": "App title in app bar"
  },
  "welcome": "Welcome",
  "login": "Login",
  "logout": "Logout",
  "greeting": "Hello, {name}!",
  "@greeting": {
    "placeholders": {
      "name": {"type": "String"}
    }
  },
  "itemCount": "{count, plural, =0{No items} =1{1 item} other{{count} items}}",
  "@itemCount": {
    "placeholders": {
      "count": {"type": "int"}
    }
  }
}
```

`lib/l10n/app_hi.arb`:

```json
{
  "@@locale": "hi",
  "appTitle": "फ्लटर ऐप",
  "welcome": "स्वागत है",
  "login": "लॉगिन",
  "logout": "लॉगआउट",
  "greeting": "नमस्ते, {name}!",
  "itemCount": "{count, plural, =0{कोई आइटम नहीं} =1{1 आइटम} other{{count} आइटम}}"
}
```

---

## Generate Code

```bash
flutter gen-l10n
```

Generates `lib/l10n/app_localizations.dart`.

---

## MaterialApp Setup

```dart
import 'package:flutter_localizations/flutter_localizations.dart';
import 'l10n/app_localizations.dart';

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => _MyAppState();

  static _MyAppState? of(BuildContext context) =>
      context.findAncestorStateOfType<_MyAppState>();
}

class _MyAppState extends State<MyApp> {
  Locale _locale = const Locale('en');

  void setLocale(Locale locale) {
    setState(() => _locale = locale);
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      locale: _locale,
      supportedLocales: const [
        Locale('en'),
        Locale('hi'),
      ],
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      home: const HomeScreen(),
    );
  }
}
```

---

## Use Translations

```dart
class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context)!;

    return Scaffold(
      appBar: AppBar(title: Text(l10n.appTitle)),
      body: Column(
        children: [
          Text(l10n.welcome),
          ElevatedButton(onPressed: () {}, child: Text(l10n.login)),
          Text(l10n.greeting('Ashutosh')),                 // Hello, Ashutosh!
          Text(l10n.itemCount(0)),                         // No items
          Text(l10n.itemCount(1)),                         // 1 item
          Text(l10n.itemCount(5)),                         // 5 items
        ],
      ),
    );
  }
}
```

---

## Number / Date Formatting (intl)

```dart
import 'package:intl/intl.dart';

// Currency
NumberFormat.currency(locale: 'hi', symbol: '₹').format(1234.56)
// → ₹1,234.56

// Date
DateFormat.yMMMMd('hi').format(DateTime.now())
// → 14 मार्च 2026

DateFormat.yMMMMd('en').format(DateTime.now())
// → March 14, 2026

DateFormat.jm('hi').format(DateTime.now())
// → 3:30 अ
```

---

## Accessibility (a11y)

Apps ko **disabled users** ke liye usable banana — screen readers, voice control, large text.

### Why?

- 1B+ users worldwide with disabilities
- Legal requirement (ADA, EU Accessibility Act)
- Better UX for everyone

---

## Semantic Labels

```dart
IconButton(
  icon: const Icon(Icons.shopping_cart),
  onPressed: () {},
  tooltip: 'View cart',                    // a11y label
)

Image.network(
  url,
  semanticLabel: 'Restaurant cover photo',
)

Container(
  child: Semantics(
    label: 'Cart total: 250 rupees',
    child: const Text('₹250'),
  ),
)
```

---

## Contrast & Touch Targets

```dart
// Minimum touch target — 48×48dp
SizedBox(
  width: 48,
  height: 48,
  child: IconButton(...),
)

// WCAG AA contrast — text/background ratio 4.5:1 minimum
Text(
  'Important',
  style: TextStyle(color: Colors.black, fontSize: 16),  // dark on light = high contrast
)
```

---

## Excluding from Accessibility

```dart
ExcludeSemantics(
  child: DecorativeImage(),    // background image, not semantically meaningful
)
```

---

## Test with Screen Reader

- **Android:** Settings → Accessibility → TalkBack
- **iOS:** Settings → Accessibility → VoiceOver

App khol ke har element pe touch karo — labels speak honi chahiye.

---

# 🌆 HOUR 2 — PRACTICE

## Task 1: Hindi/English App with Toggle (40 min)

### l10n/app_en.arb

```json
{
  "@@locale": "en",
  "appTitle": "Flutter Bilingual App",
  "welcome": "Welcome",
  "selectLanguage": "Select Language",
  "english": "English",
  "hindi": "हिंदी",
  "totalItems": "{count, plural, =0{No items} =1{1 item} other{{count} items}}",
  "@totalItems": {
    "placeholders": {"count": {"type": "int"}}
  }
}
```

### l10n/app_hi.arb

```json
{
  "@@locale": "hi",
  "appTitle": "फ्लटर द्विभाषी ऐप",
  "welcome": "स्वागत है",
  "selectLanguage": "भाषा चुनें",
  "english": "English",
  "hindi": "हिंदी",
  "totalItems": "{count, plural, =0{कोई आइटम नहीं} =1{1 आइटम} other{{count} आइटम}}"
}
```

### Run Code Gen

```bash
flutter gen-l10n
flutter pub get
```

### main.dart

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'l10n/app_localizations.dart';

void main() => runApp(const MyApp());

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => MyAppState();

  static MyAppState? of(BuildContext c) => c.findAncestorStateOfType<MyAppState>();
}

class MyAppState extends State<MyApp> {
  Locale _locale = const Locale('en');

  void setLocale(Locale locale) => setState(() => _locale = locale);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      locale: _locale,
      supportedLocales: const [Locale('en'), Locale('hi')],
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      theme: ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo)),
      home: const HomeScreen(),
    );
  }
}

class HomeScreen extends StatefulWidget {
  const HomeScreen({super.key});
  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  int _count = 0;

  @override
  Widget build(BuildContext context) {
    final l10n = AppLocalizations.of(context)!;

    return Scaffold(
      appBar: AppBar(title: Text(l10n.appTitle)),
      body: Padding(
        padding: const EdgeInsets.all(20),
        child: Column(
          children: [
            // Language selector
            Card(
              child: Padding(
                padding: const EdgeInsets.all(16),
                child: Column(
                  children: [
                    Text(l10n.selectLanguage, style: const TextStyle(fontWeight: FontWeight.bold)),
                    const SizedBox(height: 12),
                    Wrap(
                      spacing: 8,
                      children: [
                        ElevatedButton(
                          onPressed: () => MyApp.of(context)?.setLocale(const Locale('en')),
                          child: Text(l10n.english),
                        ),
                        ElevatedButton(
                          onPressed: () => MyApp.of(context)?.setLocale(const Locale('hi')),
                          child: Text(l10n.hindi),
                        ),
                      ],
                    ),
                  ],
                ),
              ),
            ),
            const SizedBox(height: 24),
            Text(l10n.welcome, style: const TextStyle(fontSize: 28, fontWeight: FontWeight.bold)),
            const SizedBox(height: 24),
            Text(l10n.totalItems(_count), style: const TextStyle(fontSize: 22)),
            const SizedBox(height: 24),
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(onPressed: () => setState(() => _count++), child: const Text('+ Add')),
                const SizedBox(width: 8),
                ElevatedButton(onPressed: () => setState(() => _count = (_count - 1).clamp(0, 100)), child: const Text('- Remove')),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
```

> **Test:** Hindi button click → सब labels Hindi me. Plural rules bhi Hindi ke according work karenge.

---

## Task 2: Accessibility Improvements (15 min)

```dart
class AccessibleScreen extends StatelessWidget {
  const AccessibleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Accessible UI'),
        actions: [
          IconButton(
            icon: const Icon(Icons.notifications),
            onPressed: () {},
            tooltip: 'View notifications',                  // ← screen reader speaks
          ),
        ],
      ),
      body: ListView(
        children: [
          // Profile picture with label
          Center(
            child: Padding(
              padding: const EdgeInsets.all(20),
              child: Semantics(
                label: 'User profile photo of Ashutosh',
                child: const CircleAvatar(radius: 50, child: Icon(Icons.person, size: 60)),
              ),
            ),
          ),

          // Decorative divider — exclude
          ExcludeSemantics(
            child: Container(
              height: 4,
              decoration: const BoxDecoration(
                gradient: LinearGradient(colors: [Colors.indigo, Colors.purple]),
              ),
            ),
          ),

          // Important info — labelled
          Semantics(
            label: 'Account balance: 1500 rupees',
            child: Container(
              padding: const EdgeInsets.all(16),
              child: const Text('₹1,500', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
            ),
          ),

          // Touch target — minimum 48dp
          Padding(
            padding: const EdgeInsets.all(16),
            child: Material(
              color: Colors.indigo,
              child: InkWell(
                onTap: () {},
                child: const SizedBox(
                  width: double.infinity,
                  height: 56,
                  child: Center(
                    child: Text('LARGE TOUCHABLE BUTTON', style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
                  ),
                ),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

---

## Task 3: Test with Screen Reader (10 min)

### Android (TalkBack)

1. Settings → Accessibility → TalkBack ON
2. App open karo
3. Each element pe **single tap** — read karega
4. **Double tap** — actually click

### iOS (VoiceOver)

1. Settings → Accessibility → VoiceOver ON
2. Same — single tap → speak, double tap → click

> **Verify:** Sab IconButton tooltips, image semanticLabels, touch targets accessible hain.

---

## Task 4: Git Commit (5 min)

```bash
git add .
git commit -m "Day 44: i18n (Hindi/English) + accessibility improvements"
git push
```

---

## 📋 Quick Revision

| Concept | Code |
|---------|------|
| Setup | `flutter_localizations` + `intl` + `l10n.yaml` |
| ARB | `lib/l10n/app_<lang>.arb` JSON |
| Generate | `flutter gen-l10n` |
| Use | `AppLocalizations.of(context)!.key` |
| Plural | `{count, plural, =0{...} other{...}}` |
| Locale | `MaterialApp(locale: ..., supportedLocales: ...)` |
| Tooltip | `IconButton(tooltip: 'Label')` |
| Semantics | `Semantics(label: '...', child: ...)` |
| Hide from a11y | `ExcludeSemantics` |
| Touch target | min 48×48 |

---

## 🎯 Interview Tips

1. **Q: ARB file format kyu?**
   *A: Standard JSON-based with placeholders + plural support. Tools (Crowdin, Localize) compatible.*

2. **Q: Plural strings kaise handle karte hain?**
   *A: `{count, plural, =0{none} =1{one} other{many}}` — locale-aware (Hindi me different rules).*

3. **Q: Accessibility kab consider karte ho?**
   *A: Day 1 se — tooltip on icons, semantic labels for images, sufficient contrast. Easier than retrofitting.*

---

## ✅ Aaj Kya Sikha

✅ flutter_localizations setup
✅ ARB files for translations
✅ flutter gen-l10n code generation
✅ MaterialApp locale config
✅ Plural + placeholder handling
✅ NumberFormat + DateFormat (intl)
✅ Semantic labels + tooltips
✅ Touch targets + contrast
✅ TalkBack/VoiceOver testing

**Kal (Day 45):** Testing Pro — golden tests, mocktail, integration tests.
