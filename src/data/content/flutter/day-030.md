# Day 30: Capstone Day 5 — Sign APK + Play Store Deploy

> **Aaj ka 2-hour plan:**
> - **Hour 1:** App icon + splash, keystore generate, signing config, build release AAB
> - **Hour 2:** Play Console setup, internal testing track, upload, share testing link

**Phase:** Phase 3 — Advanced (Capstone Day 5 of 5 — **FINAL DAY** 🎉)

---

# 🌅 HOUR 1 — APP SIGNING

## Step 1: App Icon + Splash

```bash
flutter pub add --dev flutter_launcher_icons
flutter pub add --dev flutter_native_splash
```

Add icon `assets/icon/app_icon.png` (1024×1024). Splash: `assets/splash/splash.png`.

`pubspec.yaml`:

```yaml
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  background_color_ios: "#FF6B35"
  adaptive_icon_background: "#FF6B35"

flutter_native_splash:
  color: "#FF6B35"
  image: assets/splash/splash.png
  android_12:
    color: "#FF6B35"
    image: assets/splash/splash.png
```

Run:

```bash
dart run flutter_launcher_icons
dart run flutter_native_splash:create
```

---

## Step 2: Update App Name + Bundle ID

### Android (`android/app/build.gradle`)

```gradle
android {
    namespace "com.yourname.foodordering"

    defaultConfig {
        applicationId "com.yourname.foodordering"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }
}
```

### Display name

`android/app/src/main/AndroidManifest.xml`:

```xml
<application
    android:label="Food Ordering"
    ...>
```

### iOS (`ios/Runner.xcodeproj`)

Xcode → Runner → General → Display Name + Bundle Identifier

---

## Step 3: Generate Keystore

```bash
keytool -genkey -v -keystore ~/upload-keystore.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias upload
```

Inputs:
- Password: **Strong password yaad rakho!** (lost = can't update app ever)
- Name, org, etc. (anything OK)

> **CRITICAL:** Keystore file aur password **lifetime backup** rakho. Lost = Play Store me app update karna **impossible** ho jayega.

Save:
- `~/upload-keystore.jks` → secure location (Google Drive, password manager)

---

## Step 4: Configure Signing

Create `android/key.properties` (DO NOT COMMIT):

```properties
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=upload
storeFile=/Users/yourname/upload-keystore.jks
```

Add to `.gitignore`:
```
android/key.properties
*.jks
```

### Update `android/app/build.gradle`

Top of file:
```gradle
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}
```

Inside `android { ... }`:
```gradle
signingConfigs {
    release {
        keyAlias keystoreProperties['keyAlias']
        keyPassword keystoreProperties['keyPassword']
        storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
        storePassword keystoreProperties['storePassword']
    }
}

buildTypes {
    release {
        signingConfig signingConfigs.release
        minifyEnabled true
        shrinkResources true
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
}
```

### Add ProGuard rules (`android/app/proguard-rules.pro`)

```
# Firebase
-keep class com.google.firebase.** { *; }
-keep class com.google.android.gms.** { *; }

# Play Core (for split APKs)
-keep class com.google.android.play.core.** { *; }
```

---

## Step 5: Build Signed AAB

```bash
flutter clean
flutter build appbundle --release
```

Output:
```
Built build/app/outputs/bundle/release/app-release.aab
```

> **Why AAB not APK?** Play Store **AAB only** accept karta hai 2021 ke baad. Smaller install size + automatic per-device optimization.

### Test Signed Build Locally

```bash
flutter build apk --release
# Install on real device
flutter install --release
```

---

# 🌆 HOUR 2 — PLAY STORE DEPLOY

## Step 6: Google Play Developer Account

1. [play.google.com/console](https://play.google.com/console)
2. **One-time $25 fee** (lifetime)
3. Identity verification (govt ID + photo)
4. Account approval — 24-48 hrs

> **Tip:** Account create karte hi shuru karo — verification time lagta hai.

---

## Step 7: Create App in Play Console

1. **Play Console → Create app**
2. App details:
   - **App name:** Food Ordering
   - **Default language:** English (India) ya Hindi
   - **App or game:** App
   - **Free or paid:** Free
   - Accept declarations

---

## Step 8: Set Up App (Mandatory Sections)

Play Console me red badges wale sections complete karo:

### Privacy Policy

Required if app collects data. Quick way: [privacypolicies.com](https://privacypolicies.com) free generate.

### App Access

If login is required, mention test credentials:
- Demo email: `test@app.com`
- Password: `Test123!`

### Ads

"This app contains ads" → No (likely)

### Content Rating

Questionnaire fill karo (e.g., violence, drugs, etc.) → Likely "Everyone"

### Target Audience

Age groups select karo (likely 13+ for general apps)

### Data Safety

Honest fill karo:
- **Personal info collected:** Email, name (for auth)
- **Location:** if location feature
- **Encrypted in transit:** Yes (Firebase HTTPS)

### App Category

→ Food & Drink

### Store Listing

- **Short description:** "Order food from your favorite restaurants"
- **Full description:** App features list, key benefits
- **App icon:** 512×512 PNG
- **Feature graphic:** 1024×500 PNG
- **Phone screenshots:** Min 2, max 8 (1080×1920 typical)
- **Tablet screenshots:** Optional but recommended

> **Tip:** [Canva](https://canva.com) me beautiful screenshots banao with text overlays.

---

## Step 9: Internal Testing Track

Internal testing = sirf 100 testers, no review needed (instant available).

1. **Testing → Internal testing**
2. **Create new release**
3. Upload AAB: `app-release.aab`
4. **Release name:** "1.0.0 — Initial release"
5. **Release notes:**
   ```
   First release of Food Ordering App
   - Browse restaurants
   - Add to cart
   - Place orders
   - Real-time order tracking
   ```
6. **Save → Review release → Start rollout**

### Add Testers

1. **Testers tab** → Create email list
2. Add tester emails (ya yourself)
3. **Copy opt-in URL**
4. Share URL → testers click → install from Play Store

> **Magic:** Testing me **review nahi hota**, instant install. Production me 1-7 days review.

---

## Step 10: First Production Release (Optional)

Internal me sab theek dikhe to production:

1. **Production → Create new release**
2. Same AAB upload
3. Submit for review

Review timeline: **1-7 days** (first release usually slower).

---

## Step 11: App Bundle Explorer

Play Console → **Bundle Explorer**:
- Upload kiya AAB ko explore karo
- Different device configs ke liye APK sizes dekho
- Crash reports + ANRs (later)

---

## Step 12: Crashlytics (Bonus)

```bash
flutter pub add firebase_crashlytics
```

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

  // Pass uncaught errors to Crashlytics
  FlutterError.onError = FirebaseCrashlytics.instance.recordFlutterFatalError;
  PlatformDispatcher.instance.onError = (error, stack) {
    FirebaseCrashlytics.instance.recordError(error, stack, fatal: true);
    return true;
  };

  runApp(const ProviderScope(child: MyApp()));
}
```

Test crash:
```dart
ElevatedButton(
  onPressed: () => FirebaseCrashlytics.instance.crash(),
  child: const Text('Test Crash'),
)
```

Production me real crashes Crashlytics dashboard pe dikhenge.

---

## Step 13: Final Git Commit

```bash
git add .
git commit -m "Capstone Day 5 FINAL: Signed AAB + Play Store internal testing"
git tag v1.0.0
git push origin master --tags
```

### GitHub README

```markdown
# Food Ordering App 🍔

Production Flutter app deployed on Play Store internal testing.

## Tech Stack
- Flutter 3.x + Dart 3.x
- Firebase Auth + Firestore + Storage + FCM
- Riverpod state management
- Cached Network Image

## Features
- Email + Google authentication
- Browse restaurants with menus
- Cart with quantity controls
- COD order placement
- Real-time order tracking via Firestore streams
- Push notifications (FCM)
- Order history with status timeline

## Setup
1. `flutter pub get`
2. `flutterfire configure` (your Firebase project)
3. `flutter run`

## Build
```bash
flutter build appbundle --release
```

## Screenshots
[Add 5-6 screenshots]

## Demo
[Play Store internal testing link]

---
Built during 30-Day Flutter Mastery Program
```

---

## 🎉 Phase 3 + 30-DAY COURSE COMPLETE!

**30 din me tumne sikha:**

### Phase 1 — Basic (Day 1-10)
- Dart language fundamentals
- Flutter widgets, layouts, lists
- Pehla complete app (Profile Card)

### Phase 2 — Mid (Day 11-20)
- StatefulWidget, forms, navigation
- Provider state management
- REST APIs + JSON parsing
- SharedPreferences + animations
- Weather App with API + persistence

### Phase 3 — Advanced (Day 21-30)
- Riverpod state management
- Firebase (Auth + Firestore + Storage + FCM)
- Native plugins (camera, GPS)
- Real-time data streams
- **Production app deployed to Play Store!** 🚀

---

## 🎯 Tum Ab Kya Kar Sakte Ho?

✅ Cross-platform Android + iOS apps (single codebase)
✅ Real-time database integration
✅ Firebase Auth + Firestore + Storage
✅ Push notifications (FCM)
✅ Production deployment workflow
✅ Code signing + Play Store deploy
✅ Modern state management (Riverpod)
✅ Real-world architecture (folder structure, services, providers)

**Resume me likh sakte ho:** "Built and deployed production Flutter app with Firebase backend, real-time order tracking, and 50+ users on Play Store internal testing."

---

## ⏭️ Phase 4 BONUS Preview — Senior Pro Level (Day 31-50)

Agar aur deeper jana hai:
- **Bloc pattern** (alternative to Riverpod, industry-standard)
- **Clean Architecture** (Domain/Data/Presentation layers)
- **Native interop** (Method Channels — calling Kotlin/Swift)
- **Custom plugin development**
- **Background services + Isolates**
- **Flutter Web + Desktop** (multi-platform)
- **i18n + Accessibility**
- **CI/CD with Codemagic**
- **iOS App Store deployment**
- **Pro Capstone — multi-platform app**

Phase 4 BONUS optional hai. **30 days ka core curriculum yahan complete!**

---

## ✅ Final Day Checklist

✅ App icon generated (Android + iOS)
✅ Native splash screen
✅ Bundle ID configured
✅ Keystore generated + secured backup
✅ Signed AAB build (`flutter build appbundle --release`)
✅ Play Console account ($25 paid)
✅ App created in Play Console
✅ All store listing sections filled
✅ Privacy policy uploaded
✅ Internal testing track configured
✅ Testers invited via opt-in URL
✅ Crashlytics integrated (bonus)
✅ Git tagged v1.0.0
✅ GitHub README polished

---

# 🎓 30-Day Flutter Mastery — COMPLETE!

**Mubarak ho! 🎉**

Tumne 30 din me Flutter sikha **zero se production deployed app tak**. Ye journey real, hands-on, aur portfolio-grade thi.

**Aage:**
- Real users invite karo testing me — feedback lo
- Naye features add karo (search, ratings, reviews, payments)
- Phase 4 BONUS continue karo — senior pro level
- LinkedIn pe project share karo
- Open source contribute karo Flutter ecosystem me

**Code mat rukh — banao, ship karo, sikho.** Yahi developer ka dharma hai. 🚀
