# Day 20: Mini Project — Weather App with API

> **Aaj ka 2-hour plan:**
> - **Hour 1 (Walkthrough):** Phase 2 recap, Weather app planning, OpenWeatherMap API setup, project structure
> - **Hour 2 (Build):** Full Weather app — search city, fetch weather, save favorites, animated UI

**Phase:** Phase 2 — Mid (FINAL DAY 🎉)

---

# 🌅 HOUR 1 — PROJECT PLANNING

## Phase 2 Recap

| Day | Topic |
|-----|-------|
| 11 | StatefulWidget, setState, lifecycle |
| 12 | Forms + validation |
| 13 | Buttons + themes + dark mode |
| 14 | Navigation + named routes + Hero |
| 15 | Provider state management |
| 16 | HTTP package + REST APIs |
| 17 | JSON parsing + type-safe models |
| 18 | SharedPreferences + persistence |
| 19 | Animations basics |

**Aaj Day 20:** **Sab combine** — real internet-connected app with state, persistence, animations.

---

## Project: Weather App 🌤️

**Features:**
- Search city → fetch live weather (OpenWeatherMap API)
- Show: temp, condition, humidity, wind, icon
- Add city to favorites (saved in SharedPreferences)
- Dark mode toggle
- Loading + error states
- Hero animation on tap
- Pull-to-refresh

---

## OpenWeatherMap API Setup

1. [openweathermap.org/api](https://openweathermap.org/api) pe free signup
2. Profile → API keys → copy key
3. Free tier: 1000 calls/day (kafi for learning)

**Endpoint:**
```
https://api.openweathermap.org/data/2.5/weather?q=Lucknow&units=metric&appid=YOUR_KEY
```

**Sample response:**
```json
{
  "main": {"temp": 28.5, "humidity": 60, "feels_like": 30.2},
  "weather": [{"main": "Clouds", "description": "scattered clouds", "icon": "03d"}],
  "wind": {"speed": 3.5},
  "name": "Lucknow",
  "sys": {"country": "IN"}
}
```

---

## Folder Structure

```
weather_app/
├── lib/
│   ├── main.dart
│   ├── models/
│   │   └── weather.dart
│   ├── services/
│   │   ├── weather_service.dart
│   │   └── favorites_service.dart
│   ├── providers/
│   │   └── theme_provider.dart
│   ├── screens/
│   │   ├── home_screen.dart
│   │   └── favorites_screen.dart
│   └── widgets/
│       └── weather_card.dart
└── pubspec.yaml
```

---

## Setup

```bash
flutter create weather_app
cd weather_app
flutter pub add http provider shared_preferences
```

---

# 🌆 HOUR 2 — BUILD

## Step 1: models/weather.dart

```dart
class Weather {
  final String city;
  final String country;
  final double temp;
  final double feelsLike;
  final int humidity;
  final double windSpeed;
  final String condition;
  final String description;
  final String icon;

  const Weather({
    required this.city,
    required this.country,
    required this.temp,
    required this.feelsLike,
    required this.humidity,
    required this.windSpeed,
    required this.condition,
    required this.description,
    required this.icon,
  });

  factory Weather.fromJson(Map<String, dynamic> json) {
    return Weather(
      city: json['name'] as String,
      country: json['sys']['country'] as String,
      temp: (json['main']['temp'] as num).toDouble(),
      feelsLike: (json['main']['feels_like'] as num).toDouble(),
      humidity: json['main']['humidity'] as int,
      windSpeed: (json['wind']['speed'] as num).toDouble(),
      condition: json['weather'][0]['main'] as String,
      description: json['weather'][0]['description'] as String,
      icon: json['weather'][0]['icon'] as String,
    );
  }

  String get iconUrl => 'https://openweathermap.org/img/wn/$icon@4x.png';
}
```

---

## Step 2: services/weather_service.dart

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import '../models/weather.dart';

class WeatherService {
  static const _apiKey = 'YOUR_API_KEY_HERE';  // ← yahan apna key daalo
  static const _baseUrl = 'https://api.openweathermap.org/data/2.5/weather';

  static Future<Weather> getWeather(String city) async {
    final url = Uri.parse('$_baseUrl?q=$city&units=metric&appid=$_apiKey');
    final response = await http.get(url).timeout(const Duration(seconds: 10));

    if (response.statusCode == 200) {
      return Weather.fromJson(jsonDecode(response.body));
    } else if (response.statusCode == 404) {
      throw Exception('City "$city" not found');
    } else {
      throw Exception('Error: ${response.statusCode}');
    }
  }
}
```

---

## Step 3: services/favorites_service.dart

```dart
import 'package:shared_preferences/shared_preferences.dart';

class FavoritesService {
  static const _key = 'favorite_cities';

  static Future<List<String>> getAll() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getStringList(_key) ?? [];
  }

  static Future<void> add(String city) async {
    final prefs = await SharedPreferences.getInstance();
    final list = prefs.getStringList(_key) ?? [];
    if (!list.contains(city)) {
      list.add(city);
      await prefs.setStringList(_key, list);
    }
  }

  static Future<void> remove(String city) async {
    final prefs = await SharedPreferences.getInstance();
    final list = prefs.getStringList(_key) ?? [];
    list.remove(city);
    await prefs.setStringList(_key, list);
  }

  static Future<bool> contains(String city) async {
    final prefs = await SharedPreferences.getInstance();
    final list = prefs.getStringList(_key) ?? [];
    return list.contains(city);
  }
}
```

---

## Step 4: providers/theme_provider.dart

```dart
import 'package:flutter/foundation.dart';
import 'package:shared_preferences/shared_preferences.dart';

class ThemeProvider extends ChangeNotifier {
  bool _isDark = false;
  bool get isDark => _isDark;

  Future<void> load() async {
    final prefs = await SharedPreferences.getInstance();
    _isDark = prefs.getBool('isDark') ?? false;
    notifyListeners();
  }

  Future<void> toggle() async {
    _isDark = !_isDark;
    final prefs = await SharedPreferences.getInstance();
    await prefs.setBool('isDark', _isDark);
    notifyListeners();
  }
}
```

---

## Step 5: widgets/weather_card.dart

```dart
import 'package:flutter/material.dart';
import '../models/weather.dart';

class WeatherCard extends StatelessWidget {
  final Weather weather;
  final VoidCallback? onFavorite;
  final bool isFavorite;

  const WeatherCard({
    super.key,
    required this.weather,
    this.onFavorite,
    this.isFavorite = false,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.all(16),
      padding: const EdgeInsets.all(20),
      decoration: BoxDecoration(
        gradient: const LinearGradient(
          colors: [Color(0xFF6366F1), Color(0xFF8B5CF6)],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
        borderRadius: BorderRadius.circular(20),
        boxShadow: [
          BoxShadow(color: Colors.indigo.withOpacity(0.3), blurRadius: 20, offset: const Offset(0, 8)),
        ],
      ),
      child: Column(
        children: [
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Text(weather.city, style: const TextStyle(color: Colors.white, fontSize: 28, fontWeight: FontWeight.bold)),
                  Text(weather.country, style: const TextStyle(color: Colors.white70, fontSize: 14)),
                ],
              ),
              if (onFavorite != null)
                IconButton(
                  icon: Icon(isFavorite ? Icons.favorite : Icons.favorite_border, color: Colors.white, size: 28),
                  onPressed: onFavorite,
                ),
            ],
          ),
          const SizedBox(height: 16),
          Hero(
            tag: 'weather-icon-${weather.city}',
            child: Image.network(weather.iconUrl, width: 120, height: 120),
          ),
          Text('${weather.temp.toStringAsFixed(1)}°C', style: const TextStyle(color: Colors.white, fontSize: 64, fontWeight: FontWeight.bold)),
          Text(weather.description.toUpperCase(), style: const TextStyle(color: Colors.white, fontSize: 16, letterSpacing: 1.5)),
          const SizedBox(height: 16),
          Row(
            mainAxisAlignment: MainAxisAlignment.spaceAround,
            children: [
              _stat(Icons.thermostat, 'Feels', '${weather.feelsLike.toStringAsFixed(0)}°'),
              _stat(Icons.water_drop, 'Humidity', '${weather.humidity}%'),
              _stat(Icons.air, 'Wind', '${weather.windSpeed} m/s'),
            ],
          ),
        ],
      ),
    );
  }

  Widget _stat(IconData icon, String label, String value) {
    return Column(
      children: [
        Icon(icon, color: Colors.white70, size: 24),
        const SizedBox(height: 4),
        Text(label, style: const TextStyle(color: Colors.white70, fontSize: 12)),
        Text(value, style: const TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.bold)),
      ],
    );
  }
}
```

---

## Step 6: screens/home_screen.dart

```dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../models/weather.dart';
import '../services/weather_service.dart';
import '../services/favorites_service.dart';
import '../providers/theme_provider.dart';
import '../widgets/weather_card.dart';
import 'favorites_screen.dart';

class HomeScreen extends StatefulWidget {
  const HomeScreen({super.key});

  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  final _controller = TextEditingController(text: 'Lucknow');
  Weather? _weather;
  bool _loading = false;
  String? _error;
  bool _isFavorite = false;

  @override
  void initState() {
    super.initState();
    _search();
  }

  Future<void> _search() async {
    if (_controller.text.trim().isEmpty) return;
    setState(() {
      _loading = true;
      _error = null;
    });

    try {
      final w = await WeatherService.getWeather(_controller.text.trim());
      final fav = await FavoritesService.contains(w.city);
      setState(() {
        _weather = w;
        _isFavorite = fav;
        _loading = false;
      });
    } catch (e) {
      setState(() {
        _error = e.toString().replaceAll('Exception: ', '');
        _loading = false;
        _weather = null;
      });
    }
  }

  Future<void> _toggleFavorite() async {
    if (_weather == null) return;
    if (_isFavorite) {
      await FavoritesService.remove(_weather!.city);
    } else {
      await FavoritesService.add(_weather!.city);
    }
    setState(() => _isFavorite = !_isFavorite);
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(_isFavorite ? 'Added to favorites' : 'Removed from favorites')),
    );
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final theme = context.watch<ThemeProvider>();
    return Scaffold(
      appBar: AppBar(
        title: const Text('🌤️ Weather'),
        actions: [
          IconButton(
            icon: Icon(theme.isDark ? Icons.light_mode : Icons.dark_mode),
            onPressed: () => context.read<ThemeProvider>().toggle(),
          ),
          IconButton(
            icon: const Icon(Icons.favorite),
            onPressed: () => Navigator.push(context, MaterialPageRoute(builder: (_) => const FavoritesScreen())),
          ),
        ],
      ),
      body: RefreshIndicator(
        onRefresh: _search,
        child: SingleChildScrollView(
          physics: const AlwaysScrollableScrollPhysics(),
          child: Column(
            children: [
              Padding(
                padding: const EdgeInsets.all(16),
                child: TextField(
                  controller: _controller,
                  decoration: InputDecoration(
                    hintText: 'City naam likho',
                    prefixIcon: const Icon(Icons.location_city),
                    suffixIcon: IconButton(icon: const Icon(Icons.search), onPressed: _search),
                    border: OutlineInputBorder(borderRadius: BorderRadius.circular(12)),
                  ),
                  onSubmitted: (_) => _search(),
                ),
              ),
              if (_loading) const Padding(padding: EdgeInsets.all(40), child: CircularProgressIndicator()),
              if (_error != null)
                Padding(
                  padding: const EdgeInsets.all(20),
                  child: Column(
                    children: [
                      const Icon(Icons.error_outline, size: 60, color: Colors.red),
                      const SizedBox(height: 8),
                      Text(_error!, style: const TextStyle(color: Colors.red), textAlign: TextAlign.center),
                    ],
                  ),
                ),
              if (_weather != null)
                AnimatedSwitcher(
                  duration: const Duration(milliseconds: 500),
                  child: WeatherCard(
                    key: ValueKey(_weather!.city),
                    weather: _weather!,
                    isFavorite: _isFavorite,
                    onFavorite: _toggleFavorite,
                  ),
                ),
            ],
          ),
        ),
      ),
    );
  }
}
```

---

## Step 7: screens/favorites_screen.dart

```dart
import 'package:flutter/material.dart';
import '../services/favorites_service.dart';
import '../services/weather_service.dart';
import '../models/weather.dart';

class FavoritesScreen extends StatefulWidget {
  const FavoritesScreen({super.key});

  @override
  State<FavoritesScreen> createState() => _FavoritesScreenState();
}

class _FavoritesScreenState extends State<FavoritesScreen> {
  List<Weather> _weathers = [];
  bool _loading = true;

  @override
  void initState() {
    super.initState();
    _load();
  }

  Future<void> _load() async {
    setState(() => _loading = true);
    final cities = await FavoritesService.getAll();
    final list = <Weather>[];
    for (final c in cities) {
      try {
        list.add(await WeatherService.getWeather(c));
      } catch (_) {}
    }
    if (mounted) {
      setState(() {
        _weathers = list;
        _loading = false;
      });
    }
  }

  Future<void> _remove(String city) async {
    await FavoritesService.remove(city);
    await _load();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('❤️ Favorites')),
      body: _loading
          ? const Center(child: CircularProgressIndicator())
          : _weathers.isEmpty
              ? const Center(child: Text('Koi favorite nahi'))
              : ListView.builder(
                  itemCount: _weathers.length,
                  itemBuilder: (_, i) {
                    final w = _weathers[i];
                    return Card(
                      margin: const EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                      child: ListTile(
                        leading: Image.network(w.iconUrl, width: 50, height: 50),
                        title: Text(w.city, style: const TextStyle(fontWeight: FontWeight.bold)),
                        subtitle: Text('${w.temp.toStringAsFixed(1)}°C — ${w.description}'),
                        trailing: IconButton(
                          icon: const Icon(Icons.delete, color: Colors.red),
                          onPressed: () => _remove(w.city),
                        ),
                      ),
                    );
                  },
                ),
    );
  }
}
```

---

## Step 8: main.dart

```dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'providers/theme_provider.dart';
import 'screens/home_screen.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  final theme = ThemeProvider();
  await theme.load();

  runApp(
    ChangeNotifierProvider.value(
      value: theme,
      child: const MyApp(),
    ),
  );
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    final theme = context.watch<ThemeProvider>();
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Weather App',
      themeMode: theme.isDark ? ThemeMode.dark : ThemeMode.light,
      theme: ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo)),
      darkTheme: ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo, brightness: Brightness.dark)),
      home: const HomeScreen(),
    );
  }
}
```

---

## Step 9: Run + Test (15 min)

```bash
flutter run
```

**Test checklist:**
- ✅ Default 'Lucknow' weather load hua?
- ✅ Naya city search → weather fetch hota hai?
- ✅ 'XYZ' (galat city) → error UI?
- ✅ Heart icon → favorite toggle?
- ✅ Favorites screen → saved cities + weather?
- ✅ Dark mode toggle works + persists?
- ✅ Pull-to-refresh works?
- ✅ Internet off → graceful error?
- ✅ AnimatedSwitcher karta hai weather card pe?

---

## Step 10: Polish + Git (10 min)

**Optional improvements:**
1. **Geolocator** add karke current location se auto-fetch
2. **5-day forecast** screen
3. **Background image** based on weather (sunny/rainy)
4. **App icon** + splash screen customize
5. **flutter build web** se Vercel pe deploy karo

**Commit:**
```bash
git add .
git commit -m "Day 20: Phase 2 Capstone — Weather App with API + Provider + Persistence"
git push
```

---

## 🎉 Phase 2 Complete!

**20 din me tumne sikha:**
- ✅ Stateful UI + lifecycle management
- ✅ Forms + validation patterns
- ✅ Themes + dark mode
- ✅ Multi-screen navigation + Hero
- ✅ Provider state management
- ✅ HTTP + REST APIs + JSON
- ✅ Type-safe model classes
- ✅ SharedPreferences persistence
- ✅ Animations basics
- ✅ **Real production-style app deploy ready**

---

## ⏭️ Phase 3 Preview — Advanced + Production

**Day 21-30:**
- Day 21: **Riverpod** (modern state mgmt)
- Day 22: Firebase Auth (Email + Google)
- Day 23: Cloud Firestore CRUD + streams
- Day 24: Firebase Storage + native plugins (camera, location)
- Day 25: Push Notifications (FCM)
- Day 26-30: **Capstone — Food Ordering App** with Play Store deploy

**Phase 3 me tum:** Real production app banayega with Firebase backend, push notifications, signed APK, Play Store internal testing live link!

---

## ✅ Aaj Kya Sikha (Day 20)

✅ Multi-folder project structure (models, services, providers, screens, widgets)
✅ External API integration with API key
✅ Multiple Provider integration
✅ Persisted theme + favorites
✅ Loading/error/data states (production patterns)
✅ AnimatedSwitcher + Hero in real app
✅ Pull-to-refresh
✅ **Phase 2 Capstone — production-ready Weather App**

**Phase 2 mubarak ho! Kal se Phase 3 — Firebase + Capstone Food Ordering App + Play Store deploy.**
