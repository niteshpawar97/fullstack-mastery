# Day 10: Mini Project — Profile Card / Calculator

> **Aaj ka 2-hour plan:**
> - **Hour 1 (Walkthrough):** Phase 1 revision, project planning, file structure, reusable widgets
> - **Hour 2 (Build):** Profile Card OR Calculator app — full build, polish, Git commit

**Phase:** Phase 1 — Basic (FINAL DAY 🎉)

---

# 🌅 HOUR 1 — PROJECT PLANNING

## Phase 1 Recap — 9 Din Me Kya Sikha

| Day | Topic |
|-----|-------|
| 1 | Dart variables, types, print, string interpolation |
| 2 | Operators, conditions, loops |
| 3 | Functions, closures, arrow syntax, higher-order |
| 4 | OOP — classes, inheritance, mixins |
| 5 | Null safety, collections, generics, async |
| 6 | Flutter SDK setup, first app, Hot Reload |
| 7 | StatelessWidget, MaterialApp, Scaffold |
| 8 | Layouts — Row, Column, Container, Stack |
| 9 | ListView, GridView, Images, Icons |

**Aaj Day 10:** In sab cheezein ko combine karke ek complete mini project banao.

---

## Choose Your Project

### Option A: Profile Card App 👤
Stateless, beautiful, portfolio-worthy. Components reuse karo.

**Features:**
- Hero section with photo + name + tagline
- Skills grid (3-4 columns)
- Stats cards (projects, experience, etc.)
- Contact buttons (email, phone, social)
- Theme: gradient header + clean cards

### Option B: Calculator App 🧮
Logic + UI mix. Real interactive app.

**Features:**
- Display screen
- Number buttons 0-9 in grid
- Operations (+, -, *, /, =, AC)
- Decimal point support
- Error handling (divide by 0)

> **Recommendation:** Pehli baar pe **Profile Card** banao — UI-heavy, sab Phase 1 widgets use honge. Calculator me state management chahiye (Day 11+ me sikhenge), but basic try kar sakte ho.

---

## Project Setup

```bash
# Naya project
flutter create profile_card_app
cd profile_card_app

# Optional packages (image cache ke liye)
flutter pub add cached_network_image
```

### Folder Structure (Recommended)

```
lib/
├── main.dart                    ← entry point + MaterialApp
├── screens/
│   └── profile_screen.dart      ← main screen
├── widgets/
│   ├── profile_header.dart      ← hero section
│   ├── skill_chip.dart          ← reusable skill tag
│   ├── stat_card.dart           ← reusable stat card
│   └── contact_button.dart      ← reusable button
└── data/
    └── profile_data.dart        ← static profile info
```

> **Tip:** Files ko **screens/, widgets/, data/** me organize karo. Bada project me ye structure life-saver hai.

---

## Profile Card — Plan

### Layout Sketch

```
┌─────────────────────────────────────┐
│  [Gradient Header]                   │
│       👤 Photo                       │
│       Ashutosh Kumar                 │
│       Flutter Developer              │
│       📍 Lucknow, India              │
├─────────────────────────────────────┤
│  Skills                              │
│  [Dart] [Flutter] [Firebase]         │
│  [Git] [REST API]                    │
├─────────────────────────────────────┤
│  ┌────┐  ┌────┐  ┌────┐             │
│  │ 12 │  │ 50 │  │  3 │             │
│  │Proj│  │Days│  │ Yrs│             │
│  └────┘  └────┘  └────┘             │
├─────────────────────────────────────┤
│  [📧 Email]  [📞 Call]               │
│  [💼 LinkedIn]  [🐙 GitHub]          │
└─────────────────────────────────────┘
```

---

# 🌆 HOUR 2 — BUILD

## Step 1: profile_data.dart (5 min)

```dart
// lib/data/profile_data.dart
class ProfileData {
  static const String name = 'Ashutosh Kumar';
  static const String role = 'Flutter Developer';
  static const String location = 'Lucknow, India';
  static const String bio = 'Building cross-platform apps with Flutter & Dart';
  static const String avatarUrl = 'https://i.pravatar.cc/200?img=12';

  static const List<String> skills = [
    'Dart',
    'Flutter',
    'Firebase',
    'Git',
    'REST API',
    'Provider',
  ];

  static const List<Map<String, dynamic>> stats = [
    {'value': '12', 'label': 'Projects', 'icon': '🚀'},
    {'value': '50', 'label': 'Days', 'icon': '📅'},
    {'value': '3', 'label': 'Years', 'icon': '⭐'},
  ];

  static const Map<String, String> contacts = {
    'Email': 'ash@dev.in',
    'Phone': '+91 9876543210',
    'GitHub': 'github.com/ashutosh',
    'LinkedIn': 'linkedin.com/in/ashutosh',
  };
}
```

---

## Step 2: skill_chip.dart (5 min)

```dart
// lib/widgets/skill_chip.dart
import 'package:flutter/material.dart';

class SkillChip extends StatelessWidget {
  final String skill;

  const SkillChip({super.key, required this.skill});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 6),
      decoration: BoxDecoration(
        color: Colors.indigo.withOpacity(0.1),
        borderRadius: BorderRadius.circular(20),
        border: Border.all(color: Colors.indigo.withOpacity(0.3)),
      ),
      child: Text(
        skill,
        style: const TextStyle(
          color: Colors.indigo,
          fontWeight: FontWeight.w600,
          fontSize: 13,
        ),
      ),
    );
  }
}
```

---

## Step 3: stat_card.dart (10 min)

```dart
// lib/widgets/stat_card.dart
import 'package:flutter/material.dart';

class StatCard extends StatelessWidget {
  final String value;
  final String label;
  final String icon;
  final Color color;

  const StatCard({
    super.key,
    required this.value,
    required this.label,
    required this.icon,
    this.color = Colors.indigo,
  });

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        margin: const EdgeInsets.symmetric(horizontal: 4),
        padding: const EdgeInsets.symmetric(vertical: 16),
        decoration: BoxDecoration(
          color: Colors.white,
          borderRadius: BorderRadius.circular(12),
          boxShadow: [
            BoxShadow(
              color: Colors.black.withOpacity(0.06),
              blurRadius: 10,
              offset: const Offset(0, 2),
            ),
          ],
        ),
        child: Column(
          children: [
            Text(icon, style: const TextStyle(fontSize: 24)),
            const SizedBox(height: 4),
            Text(
              value,
              style: TextStyle(
                fontSize: 24,
                fontWeight: FontWeight.bold,
                color: color,
              ),
            ),
            Text(
              label,
              style: TextStyle(
                fontSize: 12,
                color: Colors.grey[600],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

---

## Step 4: profile_header.dart (15 min)

```dart
// lib/widgets/profile_header.dart
import 'package:flutter/material.dart';
import '../data/profile_data.dart';

class ProfileHeader extends StatelessWidget {
  const ProfileHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      padding: const EdgeInsets.symmetric(vertical: 40, horizontal: 20),
      decoration: const BoxDecoration(
        gradient: LinearGradient(
          colors: [Color(0xFF6366F1), Color(0xFF8B5CF6)],
          begin: Alignment.topLeft,
          end: Alignment.bottomRight,
        ),
      ),
      child: Column(
        children: [
          Container(
            decoration: BoxDecoration(
              shape: BoxShape.circle,
              border: Border.all(color: Colors.white, width: 4),
              boxShadow: [
                BoxShadow(
                  color: Colors.black.withOpacity(0.2),
                  blurRadius: 12,
                  offset: const Offset(0, 4),
                ),
              ],
            ),
            child: CircleAvatar(
              radius: 60,
              backgroundImage: NetworkImage(ProfileData.avatarUrl),
            ),
          ),
          const SizedBox(height: 16),
          const Text(
            ProfileData.name,
            style: TextStyle(
              fontSize: 28,
              fontWeight: FontWeight.bold,
              color: Colors.white,
            ),
          ),
          const SizedBox(height: 4),
          const Text(
            ProfileData.role,
            style: TextStyle(
              fontSize: 16,
              color: Colors.white70,
              fontStyle: FontStyle.italic,
            ),
          ),
          const SizedBox(height: 8),
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              const Icon(Icons.location_on, color: Colors.white70, size: 16),
              const SizedBox(width: 4),
              Text(
                ProfileData.location,
                style: const TextStyle(color: Colors.white70, fontSize: 14),
              ),
            ],
          ),
          const SizedBox(height: 12),
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 20),
            child: Text(
              ProfileData.bio,
              textAlign: TextAlign.center,
              style: TextStyle(
                color: Colors.white.withOpacity(0.9),
                fontSize: 13,
                height: 1.4,
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

---

## Step 5: contact_button.dart (10 min)

```dart
// lib/widgets/contact_button.dart
import 'package:flutter/material.dart';

class ContactButton extends StatelessWidget {
  final IconData icon;
  final String label;
  final Color color;
  final VoidCallback onTap;

  const ContactButton({
    super.key,
    required this.icon,
    required this.label,
    required this.color,
    required this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: InkWell(
        onTap: onTap,
        borderRadius: BorderRadius.circular(12),
        child: Container(
          margin: const EdgeInsets.symmetric(horizontal: 4),
          padding: const EdgeInsets.symmetric(vertical: 12),
          decoration: BoxDecoration(
            color: color.withOpacity(0.1),
            borderRadius: BorderRadius.circular(12),
            border: Border.all(color: color.withOpacity(0.3)),
          ),
          child: Column(
            children: [
              Icon(icon, color: color, size: 24),
              const SizedBox(height: 4),
              Text(
                label,
                style: TextStyle(
                  color: color,
                  fontWeight: FontWeight.w600,
                  fontSize: 12,
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

---

## Step 6: profile_screen.dart (15 min)

```dart
// lib/screens/profile_screen.dart
import 'package:flutter/material.dart';
import '../data/profile_data.dart';
import '../widgets/profile_header.dart';
import '../widgets/skill_chip.dart';
import '../widgets/stat_card.dart';
import '../widgets/contact_button.dart';

class ProfileScreen extends StatelessWidget {
  const ProfileScreen({super.key});

  void _showContactSnackbar(BuildContext context, String type, String value) {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text('$type: $value'),
        backgroundColor: Colors.indigo,
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[100],
      body: SingleChildScrollView(
        child: Column(
          children: [
            const ProfileHeader(),
            const SizedBox(height: 20),

            // Skills section
            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 20),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  const Text(
                    'Skills',
                    style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
                  ),
                  const SizedBox(height: 12),
                  Wrap(
                    spacing: 8,
                    runSpacing: 8,
                    children: ProfileData.skills
                        .map((s) => SkillChip(skill: s))
                        .toList(),
                  ),
                ],
              ),
            ),
            const SizedBox(height: 24),

            // Stats section
            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 16),
              child: Row(
                children: ProfileData.stats
                    .map((s) => StatCard(
                          value: s['value'],
                          label: s['label'],
                          icon: s['icon'],
                        ))
                    .toList(),
              ),
            ),
            const SizedBox(height: 24),

            // Contact section
            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 16),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  const Padding(
                    padding: EdgeInsets.symmetric(horizontal: 4),
                    child: Text(
                      'Connect',
                      style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
                    ),
                  ),
                  const SizedBox(height: 12),
                  Row(
                    children: [
                      ContactButton(
                        icon: Icons.email,
                        label: 'Email',
                        color: Colors.red,
                        onTap: () => _showContactSnackbar(context, 'Email', ProfileData.contacts['Email']!),
                      ),
                      ContactButton(
                        icon: Icons.phone,
                        label: 'Call',
                        color: Colors.green,
                        onTap: () => _showContactSnackbar(context, 'Phone', ProfileData.contacts['Phone']!),
                      ),
                      ContactButton(
                        icon: Icons.code,
                        label: 'GitHub',
                        color: Colors.black,
                        onTap: () => _showContactSnackbar(context, 'GitHub', ProfileData.contacts['GitHub']!),
                      ),
                      ContactButton(
                        icon: Icons.work,
                        label: 'LinkedIn',
                        color: Colors.blue,
                        onTap: () => _showContactSnackbar(context, 'LinkedIn', ProfileData.contacts['LinkedIn']!),
                      ),
                    ],
                  ),
                ],
              ),
            ),
            const SizedBox(height: 40),
          ],
        ),
      ),
    );
  }
}
```

---

## Step 7: main.dart (5 min)

```dart
// lib/main.dart
import 'package:flutter/material.dart';
import 'screens/profile_screen.dart';

void main() => runApp(const MyApp());

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Profile Card',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo),
        useMaterial3: true,
        scaffoldBackgroundColor: Colors.grey[100],
      ),
      home: const ProfileScreen(),
    );
  }
}
```

---

## Step 8: Run + Test (10 min)

```bash
flutter run
```

**Test checklist:**
- ✅ Hero section gradient + photo dikha?
- ✅ Skills wrap properly hote hain (multi-line)?
- ✅ Stats row me 3 cards equal width?
- ✅ Contact buttons tap karne pe snackbar?
- ✅ Scroll smooth hai?
- ✅ Phone rotate karne pe layout sahi rehta hai?

---

## Step 9: Polish (15 min)

**Optional improvements:**

1. **App bar add karo** with menu/share buttons
2. **Hover effects** desktop ke liye
3. **Asset image** use karo profile pic ke liye (instead of network)
4. **Dark mode** support
5. **Animation** — `AnimatedContainer` for stat cards on tap

---

## Step 10: Git Commit + Push (5 min)

```bash
git init
git add .
git commit -m "Day 10: Phase 1 Capstone — Profile Card App"

# GitHub pe push (agar repo bana hai)
git branch -M main
git remote add origin <your-github-url>
git push -u origin main
```

---

## 🎉 Phase 1 Complete!

10 din me tumne sikha:
- ✅ Dart language fundamentals (variables, OOP, async)
- ✅ Flutter SDK setup + first app
- ✅ Stateless widgets, MaterialApp, Scaffold
- ✅ Layouts: Row, Column, Container, Stack
- ✅ Lists, Grids, Images, Icons
- ✅ Reusable widget creation
- ✅ Project structure (screens/widgets/data folders)
- ✅ **Pehla complete app build kiya!**

---

## 🎯 Phase 1 Capstone Showcase

Apne Profile Card app ka:
- 📱 **Screenshot** lo (phone + landscape)
- 📹 **Video** record karo (5-10 sec)
- 📝 GitHub README me embed karo

> **Pro Tip:** GitHub repo me README.md banao with screenshot + tech stack list. **Resume ke liye perfect first project**.

---

## ⏭️ Phase 2 Preview — Mid Level

**Day 11-20 — Real Apps Banane Ki Shuruaat:**
- Day 11: **StatefulWidget** (interactive UI)
- Day 12: Forms + Validation (Login forms)
- Day 13: Themes + Custom Styling
- Day 14: **Navigation** (multi-screen apps)
- Day 15: Provider (state management)
- Day 16: REST API calls (HTTP)
- Day 17: JSON parsing + Models
- Day 18: SharedPreferences (local storage)
- Day 19: Animations
- Day 20: Mini Project — **Weather App / Todo App with API**

**Phase 2 ke end tak:** Real-world apps jo internet se data fetch karein, persist karein, interactive ho.

---

## ✅ Aaj Kya Sikha (Day 10)

✅ Project planning + folder structure
✅ Reusable widgets (SkillChip, StatCard, ContactButton)
✅ Static data file pattern
✅ Combining 9 din ki concepts
✅ **Complete Profile Card app deploy ready**

**Phase 1 mubarak ho! 🎉 Kal se Phase 2 — interactive apps banayenge.**
